@model Helper.Areas.Admin.Models.ViewModels.News.CreateArticleViewModel

@{
    ViewData["Title"] = "CreateArticle";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

@section Style{


}


<div class="card">
    <div class="card-body">
        <h3 class="mb-3"> فرم ایجاد مقاله</h3>

        <form asp-action="CreateArticle" class="needs-validation" novalidate="" enctype="multipart/form-data">

            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-row">
                <div class="col-md-8 mb-3">
                    <img class="d-block " id="UploadedImage" />
                </div>
            </div>


            <!-- input -->
            <div class="form-row">
                <div class="col-md-9 mb-3">
                    <label asp-for="Title" for="validationCustom03" class="text-dark"></label>
                    <input asp-for="Title" class="form-control" id="validationCustom03s"
                           placeholder="عنوان">
                    <span asp-validation-for="Title" class="text-danger"> </span>
                </div>
            </div>



            @*<div class="form-row">
            <div class="col-md-6 mb-3">
                <label asp-for="NewsType" for="validationCustom03"></label>
                <select asp-for="NewsType" class="form-control" asp-items="Html.GetEnumSelectList<Helper.Models.Enums.UserGender>()">
                    <option selected="selected" value="">Please select</option>
                </select>
                <span asp-validation-for="NewsType" class="text-danger"> </span>
            </div>
        </div>*@



            <!-- fileUpload checkBox && selectOption -->
            @*<input asp-for="ArticlePhoto" class="custom-file-input" onchange="readURL(this);" id="customFile">*@
           
            <div class="form-row">
                <div class="col-md-9 mb-3">
                    <div>
                        <input asp-for="ArticlePhoto" class="custom-file-input" onchange="readURL(this);" id="customFile">
                    </div>
                    <div class="custom-file">
                        <label class="form-contro custom-file-label" for="customFile">انتخاب  عکس مقاله </label>
                    </div>
                    <span asp-validation-for="ArticlePhoto" class="text-danger"> </span>
                </div>
            </div>



            <div class="form-row">
                <div class="col-md-9 mb-3">
                    <label asp-for="Description" for="Description"></label>
                    <textarea asp-for="Description" type="" class="form-control" id="Description"
                              placeholder="متن مقاله ...">
                    </textarea>
                    <span asp-validation-for="Description" class="text-danger"> </span>
                </div>
            </div>




            <button type="submit" class="btn btn-primary">ارسال</button>
            <a asp-action="Index" class="btn btn-success">بازگشت به لیست</a>

        </form>

    </div>
</div>



@section Scripts{
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script src="~/lib/ckeditor/ckeditor.js"></script>


    <script>

        //  اعتبارسنجی فرم
        window.addEventListener('load', function () {
            // Fetch all the forms we want to apply custom Bootstrap validation styles to
            var forms = document.getElementsByClassName('needs-validation');
            // Loop over them and prevent submission
            var validation = Array.prototype.filter.call(forms, function (form) {
                form.addEventListener('submit', function (event) {
                    if (form.checkValidity() === false) {
                        event.preventDefault();
                        event.stopPropagation();
                    }
                    form.classList.add('was-validated');
                }, false);
            });
        }, false);



        //فعال سازی ckeditor
        $(function () {
            CKEDITOR.replace('Description');
        });




        function readURL(input) {
            if (input.files && input.files[0]) {
                var reader = new FileReader();

                reader.onload = function (e) {
                    $('#UploadedImage')
                        .attr('src', e.target.result)
                        .width(500)
                        .height(300);
                };

                reader.readAsDataURL(input.files[0]);
            }
        }


        
    </script>

}
